/*: ## Lesson Plan (План лекции) */
//:
/*: [The Swift Programming Language (Swift 2.2)](https://developer.apple.com/library/ios/documentation/Swift/Conceptual/Swift_Programming_Language/ClassesAndStructures.html#//apple_ref/doc/uid/TP40014097-CH13-ID82) */
/*: [Классы и структуры](http://swiftbook.ru/doc/classes-and-structures) */
/*:
 1. Classes and Structures (Классы и структуры)
    1. Comparing Classes and Structures (Сравнение классов и структур)
        1. Definition Syntax (Синтаксис объявления)
        2. Class and Structure Instances (Экземпляры класса и структуры)
        3. Accessing Properties (Доступ к свойствам)
        4. Memberwise Initializers for Structure Types (Поэлементные инициализаторы структурных типов)
    
    2. Structures and Enumerations Are Value Types (Структуры и перечисления - типы данных)
    3. Classes Are Reference Types (Классы - ссылочный тип)
        1. Identity Operators (Операторы тождественности)
        2. Pointers (Указатели)
    4. Choosing Between Classes and Structures (Выбираем между классом и структурой)
    5. Assignment and Copy Behavior for Strings, Arrays, and Dictionaries (Присваивание и копирование поведения для строк, массивов и словарей)
 
 
 2. Properties (Свойства)
    1. Stored Properties (Хранимые свойства)
        1. Stored Properties of Constant Structure Instances (Хранимые свойства неизменяемых экземпляров структуры)
        2. Lazy Stored Properties (Ленивые хранимые свойства)
        3. Stored Properties and Instance Variables (Хранимые свойства и переменные экземпляра)
    2. Computed Properties (Вычисляемые свойства)
        1. Shorthand Setter Declaration (Сокращенный вариант объявления сеттера)
        2. Read-Only Computed Properties (Вычисляемые свойства только для чтения)
    3. Property Observers (Наблюдатели свойства)
    4. Global and Local Variables (Глобальные и локальные переменные)
    5. Type Properties (Тип Свойства)
        1. Type Property Syntax (Синтаксис типа Свойства)
        2. Querying and Setting Type Properties (Запрос и установка типа Свойства)
 
 
 3. Methods (Методы)
    1. Instance Methods (Методы экземпляра)
        1. Local and External Parameter Names for Methods (Локальные и внешние имена методов)
        2. Modifying External Parameter Name Behavior for Methods (Изменение поведения имени внешнего параметр для методов)
        3. The self Property (Свойство self)
        4. Modifying Value Types from Within Instance Methods (Изменение типов значений методами экземпляра)
        5. Assigning to self Within a Mutating Method (Присваивание значения для self внутри изменяющегося метода)
    2. Type Methods (Тип Методы)
 
 
 4. Subscripts (Индексы)
    1. Subscript Syntax (Индексный синтаксис)
    2. Subscript Usage (Использование индекса)
    3. Subscript Options (Опции индекса)

 */
