var config = {
  port: 8080,
  prefix: "/api",
  itemsCount: 100,
  random: false,
  sortRandom: false, // true only if random is false
  incrementItems: false // adds item on every request, true only if random is false
};
module.exports = config;