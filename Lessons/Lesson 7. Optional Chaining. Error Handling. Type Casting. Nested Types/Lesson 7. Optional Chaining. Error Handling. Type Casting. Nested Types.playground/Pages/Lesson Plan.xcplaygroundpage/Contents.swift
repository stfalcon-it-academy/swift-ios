/*: ## Lesson Plan (План лекции) */
//:
/*:
 1. Optional Chaining (Опциональная последовательность)
    1. Optional Chaining as an Alternative to Forced Unwrapping (Опциональная последовательность как альтернатива принудительному извлечению)
    2. Defining Model Classes for Optional Chaining (Определение классовых моделей для опциональной последовательности)
    3. Accessing Properties Through Optional Chaining (Доступ к свойствам через опциональную последовательность)
    4. Calling Methods Through Optional Chaining (Вызов методов через опциональную последовательность)
    5. Accessing Subscripts Through Optional Chaining (Доступ к индексам через опциональную последовательность)
        1. Accessing Subscripts of Optional Type (Получение доступа к сабскрипту (индексу) опционального типа)
    6. Linking Multiple Levels of Chaining (Соединение нескольких уровней ОП)
    7. Chaining on Methods with Optional Return Values (Прикрепление методов к ОП с опциональными возвращаемыми значениями)

 
 2. Error Handling (Обработка ошибок или Error-Handling)
    1. Representing and Throwing Errors (Отображение и генерация ошибок)
    2. Handling Errors (Обработка ошибок)
        1. Propagating Errors Using Throwing Functions (Передача ошибки с помощью функции throw)
        2. Handling Errors Using Do-Catch (Обработка ошибок с использование do-catch)
        3. Converting Errors to Optional Values (Преобразование ошибок в опциональные значения)
        4. Disabling Error Propagation (Запрет на передачу ошибок)
    3. Specifying Cleanup Actions (Установка действий по очистке (Cleanup))
 
 
 3. Type Casting (Приведение типов)
    1. Defining a Class Hierarchy for Type Casting (Определение классовой иерархии для приведения типов)
    2. Checking Type (Проверка типа)
    3. Downcasting (Понижающее приведение)
    4. Type Casting for Any and AnyObject (Приведение типов для Any и AnyObject)
        1. AnyObject
        2. Any
 
 
 4. Nested Types (Вложенные типы)
    1. Nested Types in Action (Вложенные типы в действии)
    2. Referring to Nested Types (Ссылка на вложенные типы)
 
 */
