/*: ## Lesson Plan (План лекции) */
//:
/*:
 
 1. Functions (Функции)
    1. Defining and Calling Functions (Объявление и вызов функций)
    
    2. Function Parameters and Return Values (Параметры функции и возвращаемые значения)
        1. Functions Without Parameters (Функции без параметров)
        2. Functions With Multiple Parameters (Функции с несколькими входными параметрами)
        3. Functions Without Return Values (Функции, не возвращающие значения)
        4. Functions with Multiple Return Values (Функции, возвращающие несколько значений)
        5. Optional Tuple Return Types (Кортеж-опционал как возвращаемый тип)
    
    3. Function Parameter Names (Имена параметров функций)
        1. Specifying External Parameter Names (Внешние имена параметров)
        2. Omitting External Parameter Names (Опускание внешних имен параметров)
        3. Default Parameter Values (Значения по умолчанию для параметров)
        4. Variadic Parameters (Вариативные параметры)
        5. In-Out Parameters (Сквозные параметры)
    
    4. Function Types (Функциональные типы)
        1. Using Function Types (Использование функциональных типов)
        2. Function Types as Parameter Types (Функциональные типы в качестве типов параметров)
        3. Function Types as Return Types (Функциональные типы в качестве возвращаемых типов)
 
    5. Nested Functions (Вложенные функции)
 
 
 2. Closures (Замыкания)
    1. Closure Expressions (Замыкающие выражения)
        1. The Sort Method (Метод sort)
        2. Closure Expression Syntax (Синтаксис замыкающего выражения)
        3. Inferring Type From Context (Вывод типа из контекста)
        4. Implicit Returns from Single-Expression Closures (Неявные возвращаемые значения из замыканий с одним выражением)
        5. Shorthand Argument Names (Сокращенные имена параметров)
        6. Operator Functions (Операторные функции)
        
    2. Trailing Closures (Последующее замыкание)
    3. Capturing Values (Захват значений)
    4. Closures Are Reference Types (Замыкания являются ссылочным типом)
    5. Nonescaping Closures (Не сбегающие замыкания)
    6. Autoclosures (Автозамыкания)
 
 
 3. Enumerations (Перечисления)
    1. Enumeration Syntax (Синтаксис перечислений)
    2. Matching Enumeration Values with a Switch Statement (Сочетание значений перечисления с оператором switch)
    3. Associated Values (Связанные значения)
    4. Raw Values (Исходные значения)
        1. Implicitly Assigned Raw Values (Неявное присвоение исходных значений)
        2. Initializing from a Raw Value (Инициализация через исходное значение)
    5. Recursive Enumerations (Рекурсивные перечисления)
 
 */
